import 'package:flutter/material.dart';

class VistaWiki extends StatelessWidget {
  const VistaWiki({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Aquí irá la Wiki de cartas de AxiaWeb', style: TextStyle(fontSize: 18)),
    );
  }
}